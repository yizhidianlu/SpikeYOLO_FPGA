# sw/petalinux/project-spec/configs/config
# (subset — the rest is auto-generated by petalinux-config)

# MACHINE_NAME intentionally not set: drops HSI/DTG into the vanilla-zynq
# board path. We skipped the Digilent BSP (no 2024.1 release exists; see
# 8f2e694), so `zybo-z7-20.dtsi` isn't on disk for DTG to find — leaving
# MACHINE_NAME=zybo-z7-20 made `device-tree.bb:do_configure` fail with
# `zybo-z7-20.dtsi board file is not present in DTG` (Cloud Claude
# URGENT_ASK_7 71197be, 2026-05-28).  The XSA already encodes the PS/PL
# topology; board-specific overlays come from our own system-user.dtsi +
# spike-accel.dtsi, which don't need a registered Digilent board name.

# ---- Boot ---------------------------------------------------------------
CONFIG_SUBSYSTEM_PRIMARY_SD_PSU_SD_1_SELECT=y
CONFIG_SUBSYSTEM_BOOTARGS_AUTO=y
CONFIG_SUBSYSTEM_USER_CMDLINE="console=ttyPS0,115200 earlycon root=/dev/mmcblk0p2 rw rootwait cma=256M"

# ---- Rootfs -------------------------------------------------------------
CONFIG_SUBSYSTEM_ROOTFS_INITRAMFS=n
CONFIG_SUBSYSTEM_ROOTFS_SD=y
CONFIG_SUBSYSTEM_ROOTFS_EXT4=y
CONFIG_SUBSYSTEM_INITRAMFS_IMAGE_NAME="petalinux-image-minimal"

# ---- FPGA Manager (re-load .bit at boot) -------------------------------
CONFIG_SUBSYSTEM_FPGA_MANAGER=y

# ---- u-boot quality of life --------------------------------------------
CONFIG_SUBSYSTEM_UBOOT_AUTOCONFIG=y
